# Realitka API

## Running the server

To run the server, run:

```bash
npx apidroid@latest
```

## Getting data

- GET `/api/estates` - get all estates
- GET `/api/estates/:id` - get estate by id
